kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: publisher
  name: publisher-gopath
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: ssd
